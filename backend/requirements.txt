# FastAPI & Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.18

# Database
motor>=3.6.0

# Cache & Queue
redis==5.2.1
celery==5.4.0

# Auth & Security
PyJWT==2.10.1
bcrypt==4.2.1
cryptography>=44.0.0
passlib[bcrypt]==1.7.4  # UNUSED — safe to remove. Verified 2026-03-29. Zero imports of passlib; security.py uses bcrypt directly.

# Data Validation & Settings
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# HTTP Client
httpx==0.28.1

# Media Processing
opencv-python-headless>=4.10.0
Pillow==11.1.0

# AI / ML
ultralytics>=8.3.0
onnxruntime>=1.20.0
onnx>=1.16.0
onnxslim>=0.1.0
roboflow>=1.2.0

# Cloud & Storage
boto3==1.35.92

# Firebase
firebase-admin==6.6.0  # UNUSED — safe to remove. Verified 2026-03-29. Zero imports; fcm_service.py uses custom OAuth2 + httpx instead.

# Monitoring
sentry-sdk[fastapi]==2.19.2  # UNUSED — safe to remove. Verified 2026-03-29. Zero imports; no Sentry init anywhere. Uses Prometheus + logging.
prometheus-fastapi-instrumentator>=7.0.0

# MQTT
paho-mqtt==2.1.0  # UNUSED in backend — safe to remove from here. Verified 2026-03-29. Zero backend imports. Edge-agent has its own requirements.txt with paho-mqtt.

# Utilities
python-dateutil==2.9.0  # UNUSED — safe to remove. Verified 2026-03-29. Zero imports of dateutil; uses stdlib datetime.
orjson==3.10.13  # UNUSED — safe to remove. Verified 2026-03-29. Zero imports; FastAPI uses default JSONResponse, no ORJSONResponse configured.
