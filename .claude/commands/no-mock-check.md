Before writing any code check:
- Am I about to return hardcoded data? If yes, STOP.
- Am I about to import a mock library? If yes, STOP.
- Does this endpoint connect to real MongoDB? If no, STOP.
Only proceed when all checks pass.
